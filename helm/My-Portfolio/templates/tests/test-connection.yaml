apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "My_Portfolio.fullname" . }}-test-connection"
  labels:
    {{- include "My_Portfolio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "My_Portfolio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

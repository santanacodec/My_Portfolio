apiVersion: v1
kind: Service
metadata:
  name: {{ include "My_Portfolio.fullname" . }}
  labels:
    {{- include "My_Portfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "My_Portfolio.selectorLabels" . | nindent 4 }}
